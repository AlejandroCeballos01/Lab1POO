public class TestPoint{
    Punto = new Punto(x, y);
    //Puntos para testear
    punto = new Punto(1,1);
    otroPunto = new Punto(4,5);

    // Impresion de los diferentes métodos
      System.out.println("El punto es " + punto.toString());
      System.out.println("Su coordenada x es " + punto.getX()+ " y su coordenada y es " + p.getY());
      System.out.println("La distancia es" +punto.calcularDistancia(otroPunto));
}